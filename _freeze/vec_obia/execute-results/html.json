{
  "hash": "b89ea6b5d2143b7b2476af8a49b7d1ed",
  "result": {
    "markdown": "---\ntitle: \"Object-based time series image analysis\"\nformat: html\n---\n\n\n<a href=\"https://www.kaggle.com/code/esensing/object-based-time-series-image-analysis\" target=\"_blank\">\n<img src=\"https://kaggle.com/static/images/open-in-kaggle.svg\"/>\n</a>\n\n### Configurations to run this chapter{-}\n\n:::{.panel-tabset}\n## R\n\n::: {.cell}\n\n```{.r .cell-code}\n# load package \"tibble\"\nlibrary(tibble)\n# load packages \"sits\" and \"sitsdata\"\nlibrary(sits)\nlibrary(sitsdata)\n# set tempdir if it does not exist \ntempdir_r <- \"~/sitsbook/tempdir/R/vec_obia\"\ndir.create(tempdir_r, showWarnings = FALSE)\n```\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\n# load \"pysits\" library\nfrom pysits import *\nfrom pathlib import Path\n# set tempdir if it does not exist \ntempdir_py = Path.home() / \"sitsbook/tempdir/Python/vec_obia\"\ntempdir_py.mkdir(parents=True, exist_ok=True)\n```\n:::\n\n:::\n\n## Introduction\n\nObject-Based Image Analysis (OBIA) is an approach to remote sensing image analysis that partitions an image into closed segments which are then classified and analyzed [@Blaschke2010]. For high-resolution images (1 meter or smaller), the aim of OBIA is to create objects that represent meaningful features in the real world, like buildings, roads, and water bodies. In the case of medium-resolution images (such as Sentinel-2 or Landsat), the segments represent regions of the image with similar spectral responses, which in general do not correspond directly to individual objects on the ground. These groups of pixels are called superpixels [@Nowosad2022]. In both situations, the aim of OBIA is to obtain a spatial partition of the image into segments, each of which can then be assigned to a single class. When applicable, OBIA reduces processing time and produces labeled maps with greater spatial consistency.\n\nThe general sequence of the processes involved in OBIA in `sits` is:\n\n1.  Segmentation: The first step is to group together pixels that are similar based on a distance metric that considers the values of all bands in all time instances as well as their spatial location. We build a multitemporal attribute space where each time/band combination is taken as an independent dimension. Thus, distance metrics for segmentation in a data cube with 10 bands and 24 time steps use a 240-dimensional spectral space and a 2-dimensional Euclidean space. This distance includes both a spatial component (how far the pixel is from the center of the superpixel in terms of x and y coordinates) and a spectral component (how different the pixel’s spectral values are from the average values of the superpixel). The spectral distance is calculated using all the temporal instances of the bands. The result is a set of superpixels: clusters of spectrally similar, spatially close pixels that correspond to coherent object parts in the image. \n\n2.  Training set selection: Users should provide a training set, consisting of either labeled points or polygons. We suggest using QGIS to label some of the polygons produced by the segmentation algorithm. These labeled polygons can be used to produce a set of consistent labeled points, using `sits_get_data()`. See more at [\"Time series from data cubes\"](https://e-sensing.github.io/sitsbook/ts_basics.html#time-series-from-data-cubes).\n\n3. Model training: Using the training set produced in the previous step, users produce a classification model using `sits_train()`. See more at [Machine learning for image time series](https://e-sensing.github.io/sitsbook/classification.html).\n\n4. Classification: The next step is to classify the image using the model produced in the previous step. This step uses `sits_classify()`, which performs a pixel-based classification to obtain a set of probabilities. The segments are not used in the classification. They are saved for the next two steps: smoothing and labeling. Pixel-based classification is much faster than polygon-based classification and the results are equivalent.\n\n5. Smoothing: Smoothing for vector cubes uses a different definition of neighborhood than [raster-based probability smoothing](https://e-sensing.github.io/sitsbook/cl_smoothing.html). When dealing with raster cubes, neighborhoods are defined based on rectangular windows (e.g., 9 x 9 size). When dealing with a vector cube, neighborhoods are defined by the segments. In this case, all pixels inside a segment are considered part of the same neighborhood. When estimating the spatial effects, the probabilities of all pixels in the segment are used to calculate the smoothed probability of each pixel. \n\n6. Map labeling: Once the set of probabilities has been obtained for selected time series inside a segment, they can be used for labeling. To label the segments, `sits` takes the probabilities assigned to each pixel inside a segment and uses an aggregation function to calculate the estimated value of class probabilities for each segment. The aggregation functions available include `mean`, `median`, and `majority`. After the aggregation, `sits` selects the label of each segment based on the maximum aggregate value for the class probabilities. This step uses `sits_label_classification()`, as shown below.\n\n## Image segmentation in sits\n\nThe first step of the OBIA procedure in `sits` is to select a data cube to be segmented and a function that performs the segmentation. For this purpose, `sits` provides a generic `sits_segment()` function, which allows users to select different algorithms. The `sits_segment()` function has the following parameters:\n\n-   `cube`: a regular data cube.\n-   `seg_fn`: function to apply the segmentation\n-   `roi`: spatial region of interest in the cube\n-   `start_date`: starting date for the space-time segmentation\n-   `end_date`: final date for the space-time segmentation\n-   `memsize`: memory available for processing\n-   `multicores`: number of cores available for processing\n-   `output_dir`: output directory for the resulting cube\n-   `version`: version of the result\n-   `progress`: show progress bar?\n\nCurrently, there are two segmentation functions available: (a) `sits_snic`, which implements the Simple Non-Iterative Clustering (SNIC) algorithm; (b) `sits_slic`, which implements an extended version of the Simple Linear Iterative Clustering (SLIC). Both are described below. We suggest using `sits_snic` since it is much faster than `sits_slic` and produces good results in general. \n\n## Simple non-iterative clustering (SNIC)\n\nSNIC (Simple Non-Iterative Clustering) is a segmentation algorithm designed to partition an image into compact, uniform regions (superpixels) efficiently [@Achanta2017]. It is an evolution of the SLIC (Simple Linear Iterative Clustering) algorithm, described below. While SLIC relies on an iterative k-means approach (which requires multiple passes over the pixels), SNIC achieves similar or better results in a single pass using a priority queue, making it significantly faster and non-iterative.\n\nSNIC treats image segmentation as a region-growing problem starting from a regular grid of seed points. It uses a priority queue to determine which pixel to process next, ensuring that regions grow in the \"cheapest\" direction first.\n\nThe algorithm can be summarized as follows:\n\n1. Initialization: After choosing the data cube which will be segmented, users define the seed points (centroids) to be used to grow the superpixels. They can also choose their relative placement. The seeds can be placed on a rectangular, diamond or hexagonal grid, or can be placed randomly. A final parameter is the compactness, which defines how regular the shapes of the resulting superpixels are. \n\n2. Priority Queue: Each pixel is represented in an n-dimensional feature space, defined by all the time instances of its spectral bands and its 2-D spatial position ($x, y$). The algorithm then creates a priority queue. Initially, the 4-connected neighbors of each seed are added to the queue. The \"priority\" (or cost) is the distance from the pixel to the seed's centroid in the n-dimensional space.\n\n3. Region Growing Loop, with the following steps:\n\n(a) SNIC initializes a label map — which tracks which superpixel each pixel belongs to — along with a corresponding distance map. \n\n(b) While the priority queue is not empty, the algorithm extracts the pixel with the smallest distance (lowest cost) from the queue. \n\n(c) If the pixel has already been labeled, it is discarded; otherwise, it is assigned to the superpixel of the seed that generated this queue entry. \n\n(d) SNIC updates the centroid of that superpixel immediately after adding the new pixel. The new centroid is the average of all pixels currently in that superpixel.\n\n(e) Unlabeled neighbors of the newly added pixel are added to the priority queue. Their cost is calculated based on the current (updated) centroid of the superpixel.\n\n(f) Go to step (b) until the priority queue is empty.\n\n\n## Simple linear iterative clustering algorithm (SLIC)\n\nThe Simple Linear Iterative Clustering (SLIC) algorithm [@Achanta2012] clusters pixels into nearly uniform superpixels. This algorithm has been adapted by Nowosad and Stepinski [@Nowosad2022] to work with multispectral images. SLIC uses spectral similarity and proximity in the image space to segment the image into superpixels. Here's a high-level view of the extended SLIC algorithm:\n\n1.  The algorithm starts by dividing the image into a grid, where each cell of the grid becomes a superpixel.\n\n2.  For each cell, the pixel in the center becomes the initial \"cluster center\" for that superpixel.\n\n3.  For each pixel, the algorithm calculates a distance to each of the nearby cluster centers, using the same distance metric as that of the SNIC algorithm. \n\n4.  Each pixel is assigned to the closest cluster. After all pixels have been assigned to clusters, the algorithm recalculates the cluster centers by averaging the spatial coordinates and spectral values of all pixels within each cluster.\n\n5.  Steps 3-4 are repeated for a set number of iterations, or until the cluster assignments stop changing.\n\nThe outcome of the SLIC algorithm is a set of superpixels which try to capture the boundaries of objects within the image. \n\n## Example of SNIC-based segmentation and classification\n\nThe SNIC implementation in `sits` uses the `snic` R package. The parameters for the `sits_snic()` function are:\n\n-   `grid_seeding`: spatial organization of initial grid seeds (one of `rectangular`, `diamond`, `hexagonal`, `random`).\n-   `spacing`: distance (in number of pixels) between initial superpixels' centers.\n-   `compactness`: numerical value between 0 and 1 that controls superpixels' density. Larger values cause clusters to be more compact.\n-   `padding`: distance (in pixels) from the image borders within which no seeds are placed.\n\nTo show an example of SNIC-based segmentation, we first build a data cube, using images available in the `sitsdata` package.\n\n:::{.panel-tabset}\n## R\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/fig-obia-rgb-20LMR_1f4bdf31dc9c7e9ded9eea9b141ff946'}\n\n```{.r .cell-code}\n# directory where files are located\ndata_dir <- system.file(\"extdata/Rondonia-20LMR\", package = \"sitsdata\")\n\n# builds a cube based on existing files\ncube_20LMR <- sits_cube(\n    source = \"AWS\",\n    collection = \"SENTINEL-2-L2A\",\n    data_dir = data_dir,\n    bands = c(\"B02\", \"B8A\", \"B11\")\n)\n\n# Plot\nplot(\n    cube_20LMR, \n    red = \"B11\",\n    green = \"B8A\", \n    blue = \"B02\", \n    date = \"2022-07-16\"\n)\n```\n\n::: {.cell-output-display}\n![RGB composite image for part of tile 20LMR in Rondonia, Brasil.\n](vec_obia_files/figure-html/fig-obia-rgb-20LMR-1.png){#fig-obia-rgb-20LMR fig-align='center' width=80%}\n:::\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\n# directory where files are located\ndata_dir = r_package_dir(\"extdata/Rondonia-20LMR\", package = \"sitsdata\")\n\n# Builds a cube based on existing files\ncube_20LMR = sits_cube(\n    source = \"AWS\",\n    collection = \"SENTINEL-2-L2A\",\n    data_dir = data_dir,\n    bands = (\"B02\", \"B8A\", \"B11\")\n)\n\n# Plot\nplot(\n    cube_20LMR, \n    red = \"B11\",\n    green = \"B8A\", \n    blue = \"B02\", \n    date = \"2022-07-16\"\n)\n```\n:::\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/py-fig-obia-rgb-20LMR_737722fa28656397880726660ba5e8c1'}\n::: {.cell-output-display}\n![RGB composite image for part of tile 20LMR in Rondonia, Brasil.\n](vec_obia_files/figure-html/py-fig-obia-rgb-20LMR-1.png){fig-align='center' width=80%}\n:::\n:::\n\n:::\nThe following example produces a segmented image. For the SNIC algorithm, we take the initial separation between cluster centers (`spacing`) to be 20 pixels, the `compactness` to be 0.5, and the minimum distance between the cluster center and the image borders (`padding`) to be 10 pixels.\n\n:::{.panel-tabset}\n## R\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/fig-obia-snic-20LMR_883f8679a2a1aa97de1bc164301eceec'}\n\n```{.r .cell-code}\n# segment a cube using SNIC\n# Files are available in a local directory \nsegments_20LMR <- sits_segment(\n    cube = cube_20LMR,\n    seg_fn = sits_snic(\n        grid_seeding = \"rectangular\",\n        spacing = 20,\n        compactness = 0.5,\n        padding = 10\n    ),\n    output_dir = tempdir_r\n)\n\n# Plot segments\nplot(segments_20LMR, red = \"B11\", green = \"B8A\", blue = \"B02\",\n     date = \"2022-07-16\")\n```\n\n::: {.cell-output-display}\n![Image with segments for part of tile 20LMR in Rondonia, Brasil.\n](vec_obia_files/figure-html/fig-obia-snic-20LMR-1.png){#fig-obia-snic-20LMR fig-align='center' width=80%}\n:::\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\n# segment a cube using SLIC\n# Files are available in a local directory \nsegments_20LMR = sits_segment(\n    cube = cube_20LMR,\n    output_dir = tempdir_py,\n    seg_fn = sits_snic(\n        grid_seeding = \"rectangular\",\n        spacing = 20,\n        compactness = 0.5\n    )\n)\n\n# Plot segments\nplot(segments_20LMR, red = \"B11\", green = \"B8A\", blue = \"B02\", date = \"2022-07-16\")\n```\n:::\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/py-fig-obia-snic-20LMR_8fc4f5295ef73542bae5c4760d591171'}\n::: {.cell-output-display}\n![Image with segments for part of tile 20LMR in Rondonia, Brasil.\n](vec_obia_files/figure-html/py-fig-obia-snic-20LMR-1.png){fig-align='center' width=80%}\n:::\n:::\n\n:::\n\nIt is useful to visualize the segments in a leaflet together with the RGB image using `sits_view()`.\n\n:::{.panel-tabset}\n## R\n\n::: {.cell}\n\n```{.r .cell-code}\nsits_view(segments_20LMR, red = \"B11\", green = \"B8A\", blue = \"B02\", \n          dates = \"2022-07-16\")\n```\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\nsits_view(cube_20LMR, red = \"B11\", green = \"B8A\", blue = \"B02\", \n          dates = \"2022-07-16\")\nsits_view(segments_20LMR, add = TRUE)\n```\n:::\n\n:::\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n![Detail of segmentation of image in Amazonia](./images/segmentation_view.png){#fig-obia-view fig-align='center' width=75%}\n:::\n:::\n\n\nAfter obtaining the segments, the next step is to classify them. This is done by first training a classification model. This case study uses the training dataset `samples_deforestation_rondonia`, available in package `sitsdata`. This dataset consists of 6007 samples collected from Sentinel-2 images covering the state of Rondonia. There are nine classes: `Clear_Cut_Bare_Soil`, `Clear_Cut_Burned_Area`, `Mountainside_Forest`, `Forest`, `Riparian_Forest`, `Clear_Cut_Vegetation`, `Water`, `Wetland`, and `Seasonally_Flooded`. Each time series contains values from Sentinel-2A/2B bands B02, B03, B04, B05, B06, B07, B8A, B08, B11 and B12, from 2022-01-05 to 2022-12-23 in 16-day intervals. The samples are intended to detect deforestation events and have been used in earlier examples of the book. For the training, we select the same bands as those of the data cube.\n\n:::{.panel-tabset}\n## R\n\n::: {.cell hash='vec_obia_cache/html/obia-train-tcnn_ff33222960bdaf747e50f99fd3be9dc5'}\n\n```{.r .cell-code}\n# Select bands\nsamples_deforestation <- sits_select(samples_deforestation_rondonia,\n                                     bands = c(\"B02\", \"B8A\", \"B11\"))\n\n# Train TempCNN\ntcnn_model <- sits_train(samples_deforestation, sits_tempcnn())\n```\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\n# Load data\nsamples_deforestation_rondonia = load_samples(\n    name = \"samples_deforestation_rondonia\",\n    package = \"sitsdata\"\n)\n\n# Select bands\nsamples_deforestation = sits_select(samples_deforestation_rondonia,\n                                     bands = (\"B02\", \"B8A\", \"B11\"))\n\n# Train TempCNN\ntcnn_model = sits_train(samples_deforestation, sits_tempcnn())\n```\n:::\n\n:::\nThe segment classification procedure applies the model to every pixel inside each segment. Each pixel is then assigned a set of probability values, one for each class. The output of the procedure is a raster data cube containing a probability layer for each class. The parameters for the `sits_classify()` function for vector cubes are: \n\n* `data`: either a data cube or a set of time series.\n* `ml_model`: a trained model that uses one of the machine learning methods provided.\n* `impute_fn`: an imputation function used to interpolate NA values (default = `impute_linear`).\n* `multicores`: number of CPU cores that will be used for processing (default = 2).\n* `memsize`: RAM memory available for classification expressed in GB (default = 4).\n* `gpu_memory`: GPU memory available for classification, expressed in GB, if a GPU is available and a torch-based model is used (default = 4).\n* `batch_size`: size of batches used for processing (default = 1000 x `gpu_memory`).\n* `roi`: Region of interest where pixels will be classified: either an sf object, a shapefile, or a numeric vector in WGS 84 with named XY values (\"xmin\", \"xmax\", \"ymin\", \"ymax\") or named lat/long values (\"lon_min\", \"lat_min\", \"lon_max\", \"lat_max\"). \n* `exclusion_mask`:  Areas to be excluded from the classification process, defined by either an `sf` object or by a shapefile.\n* `start_date`: Start date for the classification process.\n* `end_date`: End date for the classification process.\n* `output_dir`: Directory where results will be stored.\n* `version`: For version control.\n* `progress`: Progress bar to track processing steps. \n\n\nIn what follows, we compute the probabilities for the classes and visualize them. Note that the probabilities are computed on a pixel-by-pixel basis. The segments are used only in the subsequent function (`sits_smooth()` and `sits_label_classification()`). \n\n:::{.panel-tabset}\n## R\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/fig-obia-probs-20LMR_dd73abaf87a3ccd582d43e256711cf0c'}\n\n```{.r .cell-code}\n# Classify the vector cube using tempcnn model\nsegments_20LMR_probs <- sits_classify(\n    data = segments_20LMR,\n    ml_model = tcnn_model,\n    output_dir = tempdir_r,\n    gpu_memory = 8,\n    memsize = 18,\n    multicores = 6,\n    version = \"tcnn\"\n)\n# Plot the probabilities for label Forest\nplot(segments_20LMR_probs, labels = \"Forest\")\n```\n\n::: {.cell-output-display}\n![Probabilities for class Forest.\n](vec_obia_files/figure-html/fig-obia-probs-20LMR-1.png){#fig-obia-probs-20LMR fig-align='center' width=80%}\n:::\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\n# Classify the vector cube\nsegments_20LMR_probs = sits_classify(\n    data = segments_20LMR,\n    ml_model = tcnn_model,\n    output_dir = tempdir_py,\n    gpu_memory = 8,\n    memsize = 18,\n    multicores = 6,\n    version = \"tcnn\"\n)\n# Plot the probabilities for label Forest\nplot(segments_20LMR_probs, labels = \"Forest\")\n```\n:::\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/py-fig-obia-probs-20LMR_d33b522208f2886b6621532514d17514'}\n::: {.cell-output-display}\n![Probabilities for class Forest.\n](vec_obia_files/figure-html/py-fig-obia-probs-20LMR-1.png){fig-align='center' width=90%}\n:::\n:::\n\n:::\n\nThen we can compute the smoothed probability cube. In the case of vector cubes, `sits_smooth()` works differently than the function does for pixel-based raster cubes. In the pixel-based case, users define a neighborhood as a fixed-sized window (e.g., 7x7 or 9x9 pixels). For vector cubes, each segment defines a neighborhood boundary. Inside the segment, `sits_smooth()` uses the `neigh_fraction` parameter to define the pixels that will be used to compute the resulting values. The smoothing method and the parameters are the same as described in the chapter [\"Bayesian smoothing for classification post-processing\"](https://e-sensing.github.io/sitsbook/cl_smoothing.html); the only difference is the neighborhood definition. The parameters are:\n\n* `cube`: Probability data cube to be smoothed;\n* `neigh_fraction`: Fraction of neighbors with high probabilities used in Bayesian inference (default = 0.5).\n* `smoothness`: Estimated variance of logit of conditional class probabilities (default = 20).\n* `exclusion_mask`:  Areas to be excluded from the classification process, defined by either an `sf` object or by a shapefile.\n* `multicores`: number of CPU cores that will be used for processing (default = 2).\n* `memsize`: RAM memory available for classification expressed in GB (default = 4).\n* `output_dir`: Directory where results will be stored.\n* `version`: For version control.\n* `progress`: Progress bar to track processing steps. \n\n:::{.panel-tabset}\n## R\n\n::: {.cell hash='vec_obia_cache/html/unnamed-chunk-9_07a7f75879879201baa4d9fdb13a53ae'}\n\n```{.r .cell-code}\nsegments_20LMR_smooth <- sits_smooth(\n    segments_20LMR_probs,\n    output_dir = tempdir_r,\n    neigh_fraction = 0.5,\n    memsize = 18,\n    multicores = 6,\n    version = \"tcnn\"\n)\n```\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\nsegments_20LMR_smooth = sits_smooth(\n    segments_20LMR_probs,\n    output_dir = tempdir_py,\n    memsize = 24,\n    multicores = 6,\n    version = \"tcnn\"\n)\n```\n:::\n\n:::\n\n\nFinally, we can compute the most likely class for each of the segments. For vector cubes, there is one additional parameter: `label_method`, which is used to define how the probabilities inside a segment will be combined. The options are `mean` (default), `median`, or `majority`. Based on the chosen option, `sits` will compute the probability for each class inside a segment. Note that all pixels inside a segment will be assigned the same probability value for each class. After this step, the function labels the segment with the class with the highest combined probability. \n\nThe parameters used in `sits_label_classification()` in case of vector cubes are:\n\n* `cube`: Probability data cube to be labeled;\n* `label_method`: Aggregation function for probabilities inside each segment (default = `mean`).\n* `multicores`: number of CPU cores that will be used for processing (default = 2).\n* `memsize`: RAM memory available for classification expressed in GB (default = 4).\n* `output_dir`: Directory where results will be stored.\n* `version`: For version control.\n* `progress`: Progress bar to track processing steps. \n\n:::{.panel-tabset}\n## R\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/fig-obia-class-20LMR_5512ff6994b7913ec9f690509ff8648d'}\n\n```{.r .cell-code}\n# Label the smoothed probability vectors\nsegments_20LMR_class <- sits_label_classification(\n    cube = segments_20LMR_smooth,\n    label_method = \"mean\",\n    memsize = 24,\n    multicores = 6,\n    output_dir = tempdir_r,\n    version = \"tcnn\"\n)\n# plot the classified segments\nplot(segments_20LMR_class, legend_position = \"inside\")\n```\n\n::: {.cell-output-display}\n![Plot of OBIA classified image.\n](vec_obia_files/figure-html/fig-obia-class-20LMR-1.png){#fig-obia-class-20LMR fig-align='center' width=90%}\n:::\n:::\n\n## Python\n\n::: {.cell}\n\n```{.python .cell-code}\n# Label the smoothed probability vectors\nsegments_20LMR_class = sits_label_classification(\n    segments_20LMR_smooth,\n    label_method = \"mean\",\n    memsize = 24,\n    multicores = 6,\n    output_dir = tempdir_py,\n    version = \"tcnn\"\n)\n# Plot the classified segments\nplot(segments_20LMR_class, legend_position = \"inside\")\n```\n:::\n\n::: {.cell layout-align=\"center\" hash='vec_obia_cache/html/py-fig-obia-class-20LMR_56bfbae0e7f7de9545ca5c03f05999f0'}\n::: {.cell-output-display}\n![Plot of OBIA classified image.\n](vec_obia_files/figure-html/py-fig-obia-class-20LMR-1.png){fig-align='center' width=90%}\n:::\n:::\n\n:::\n\n## Summary\n\nOBIA analysis applied to image time series is a worthy and efficient technique for land classification, combining the desirable sharp object boundary properties required by land use and cover maps with the analytic power of image time series. \n\n## References{-}\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}