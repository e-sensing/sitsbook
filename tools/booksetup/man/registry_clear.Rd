% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{registry_clear}
\alias{registry_clear}
\title{Clear chunk entries from the registry}
\usage{
registry_clear(chapter, chunk = NULL, registry = registry_path())
}
\arguments{
\item{chapter}{Chapter name(s) whose registry entries should be cleared.
If \code{chunk} is \code{NULL} (the default), \emph{all} entries for each chapter are
cleared.}

\item{chunk}{Optional integer vector of chunk indices to clear, scoped to
\code{chapter} (which must then be a single chapter name). If \code{NULL}, every
entry for the given chapter(s) is cleared.}

\item{registry}{Path to the YAML registry file. Defaults to
\code{registry_path()} (\verb{~/sitsbook/tempdir/.booksetup_registry.yaml}).}
}
\value{
A character vector of the registry keys that were actually
cleared, invisibly.
}
\description{
Removes one or more chunk entries from the registry file, forcing them
to be re-executed (rather than skipped) on the next data-generation run.
This is the supported way to unblock the well-known situation where an
\code{eval: false} chunk that \emph{defines} a variable was skipped (already
\code{status: "ok"} with a matching hash) while a later chunk that \emph{consumes}
that variable needs to re-run in the same session -- clearing the
defining chunk's entry forces the whole dependency chain to re-execute
together.
}
\examples{
\dontrun{
# Clear a single chunk (e.g. to force it and its dependents to re-run)
registry_clear("dc_merge", chunk = 7)

# Clear several chunks in one chapter
registry_clear("dc_merge", chunk = c(2, 7, 8))

# Clear every entry for one or more whole chapters
registry_clear(c("dc_merge", "dc_regularize"))
}
}
