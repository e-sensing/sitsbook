% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_r_chunks.R
\name{extract_r_chunks}
\alias{extract_r_chunks}
\title{Extract all R code chunks from a Quarto / R Markdown file}
\usage{
extract_r_chunks(qmd)
}
\arguments{
\item{qmd}{Path to a Quarto or R Markdown file.}
}
\value{
A named list of character vectors. Each element contains the code
lines of one R chunk. Names are chunk labels when available; otherwise
\code{chunk_1}, \code{chunk_2}, etc.
}
\description{
Reads a \code{.qmd} or \code{.Rmd} file and returns the R code it contains. All R chunks
are extracted, including those marked \verb{#| eval: false}, so that the returned
code can be used for data generation. Chunk option lines (\verb{#| ...}) are
stripped, but the actual code is preserved.
}
\details{
The returned list preserves the source location of every chunk. Each element
is a character vector with attributes \code{start_line} and \code{end_line} giving the
line numbers of the opening and closing fences in the original file,
optionally a \code{label} attribute when the chunk header contains one, and an
\code{eval} attribute taken from the chunk options (\code{TRUE} by default).
}
\examples{
sample <- system.file("extdata", "sample.qmd", package = "booksetup")
chunks <- extract_r_chunks(sample)
attr(chunks[[1]], "start_line")
}
