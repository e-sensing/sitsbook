% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{snapshot_info}
\alias{snapshot_info}
\title{Per-chunk snapshot diagnostics}
\usage{
snapshot_info(registry, snapshot_dir)
}
\arguments{
\item{registry}{A registry list, as returned by \code{\link[=registry_read]{registry_read()}}.}

\item{snapshot_dir}{Base directory for snapshots (\code{state$snapshot_dir}
from \code{\link[=new_run_state]{new_run_state()}}, typically \verb{<tempdir>/.snapshots}).}
}
\value{
A \code{data.frame} with columns \code{chapter}, \code{chunk}, \code{key},
\code{n_vars}, \code{size_bytes}, \code{status}.
}
\description{
Reports, for every chunk in \code{registry}, whether it has an associated
snapshot under \code{snapshot_dir}, how many variables it holds, and their
total size on disk. \code{eval: true} chunks and chunks whose last run
errored are never snapshotted (see \code{\link[=snapshot_chunk_env]{snapshot_chunk_env()}}) and are
reported as \code{"not_applicable"}.
}
\details{
\code{status} values:
\itemize{
\item \code{"not_applicable"}: chunk is \code{eval: true}, or its last run errored
(snapshotting does not apply).
\item \code{"missing"}: \code{eval: false} chunk completed \code{"ok"} but has no
snapshot directory at all. This is expected (not a problem) for
chunks that define no new variables (e.g. side-effect-only chunks
that just write files to disk) -- it is a \emph{signal} to investigate,
not necessarily a bug: check whether the chunk was expected to
define a bridge variable that a downstream chunk consumes.
\item \code{"empty"}: the snapshot directory exists but contains no \code{.rds}
files (e.g. every candidate variable exceeded the size guard).
\item \code{"ok"}: at least one variable was snapshotted.
}
}
