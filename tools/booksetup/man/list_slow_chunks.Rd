% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{list_slow_chunks}
\alias{list_slow_chunks}
\title{List the slowest completed chunks, optionally filtered by \code{eval}}
\usage{
list_slow_chunks(registry, eval_only = TRUE, n = Inf)
}
\arguments{
\item{registry}{A registry list, as returned by \code{\link[=registry_read]{registry_read()}}.}

\item{eval_only}{If \code{TRUE} (the default), only \code{eval: true} chunks are
returned; if \code{FALSE}, only \code{eval: false} chunks; if \code{NULL}, no filtering
on \code{eval} is applied.}

\item{n}{Maximum number of rows to return (default \code{Inf}, i.e. all).}
}
\value{
A \code{data.frame} with columns \code{chapter}, \code{chunk}, \code{lines}, \code{label},
\code{eval}, \code{elapsed}, \code{hash}, sorted by elapsed descending.
}
\description{
Handy for finding \code{eval: true} chunks (i.e. those Quarto actually
re-executes on every render) that are worth investigating or converting to
\code{eval: false} with a cached, pre-rendered output.
}
